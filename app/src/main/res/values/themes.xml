<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.CredsPo" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.CredsPo.TextInputLayout" parent="ThemeOverlay.AppCompat">
        <item name="boxStrokeWidth">0dp</item>
        <item name="boxStrokeWidthFocused">0dp</item>
        <item name="android:layout_marginTop">5dp</item>
    </style>
    <style name="Theme.CredsPo.EditTextLabel" parent="ThemeOverlay.AppCompat">
        <item name="android:textColor">@color/darker_blue</item>
        <item name="android:textSize">12sp</item>
    </style>
    <style name="Theme.CredsPo.Spinner" parent="ThemeOverlay.AppCompat">
        <item name="android:background">@drawable/edit_text_border</item>
        <item name="android:padding">5dp</item>
        <item name="android:layout_height">55dp</item>
    </style>
    <style name="Theme.CredsPo.Button" parent="ThemeOverlay.AppCompat">
        <item name="backgroundTint">@color/button</item>
        <item name="android:layout_height">55dp</item>
        <item name="android:layout_marginHorizontal">50dp</item>
    </style>
    <style name="roundedImageViewRounded">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>
</resources>